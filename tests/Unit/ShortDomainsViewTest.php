<?php

namespace Tests\Feature;

use Corals\User\Models\User;
use Illuminate\Foundation\Testing\DatabaseTransactions;
use Illuminate\Support\Facades\Auth;
use Tests\TestCase;

class ShortDomainsViewTest extends TestCase
{
    use DatabaseTransactions;

    protected function setUp(): void
    {
        parent::setUp(); // TODO: Change the autogenerated stub

        $user = User::query()->whereHas('roles', function ($query) {
            $query->where('name', 'superuser');
        })->first();
        Auth::loginUsingId($user->id);
    }

    /**
     * A basic test example.
     *
     * @return void
     */
    public function test_short_domains_view()
    {
        $response = $this->get('shortener/short-domains');

        $response->assertStatus(200)->assertViewIs('Shortener::shortDomains.index');
    }

    public function test_short_domains_create()
    {
        $response = $this->get('shortener/short-domains/create');

        $response->assertViewIs('Shortener::shortDomains.create_edit')->assertStatus(200);
    }
}
